[project]
name = "droidserver"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "flask-socketio>=5.6.1",
    "flask>=3.1.3",
]
